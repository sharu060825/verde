{
  "name": "ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "python -m compileall .",
    "lint": "python -m py_compile main.py"
  }
}
